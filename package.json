{
  "name": "stripe-webhook-proxy",
  "version": "1.0.0",
  "description": "Proxy server to add apikey header for Stripe webhooks to Supabase Edge Functions",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
